import 'package:flutter/material.dart';

class DeanDepartmentsPage extends StatelessWidget {
  const DeanDepartmentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(20.0),
      child: Center(
        child: Text('Departments Page', style: TextStyle(fontSize: 18)),
      ),
    );
  }
}